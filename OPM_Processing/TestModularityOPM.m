function TestModularityOPM(data_obj)
    %% get powerspectrum mean
    
    %% determine peak
    
    %% determine powerspectrum bootstrap samples
    
    %% determine CI spectrum
    
    %% determine distribution peak
end